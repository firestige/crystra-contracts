# 当前规范的初始来源

本目录的初始 Markdown 从 workflow-self-recursive 远端 main `3fc8aef3142bda61f63174ad9998c41c4b42c4bb` 的 docs/contracts 复制，作为 Contracts 自身维护的当前规范起点；未复制旧 release-binding JSON 或历史制品。

初始复制保持语义正文原样。品牌、链接与当前契约生成依据仍在 T1 中调整；其中旧发布时间／审批只描述既有历史，不代表 Crystra 新发布已获批准。本文是来源记录，不是 publication 或资格证明。

新资源目录由 release/config/current-resources.json 显式选择。当前新 bundle 只证明列出字节的完整性；各领域 validator 的跨仓库路径解耦与全量回归尚未完成，不应将可打包等同于已可发布。
