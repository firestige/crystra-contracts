# C095 Sidebar 交互对照

2026-09-15。用户指出 C094 仍明显偏离设计，故不以 C094 的测试通过声明 Sidebar 已验收。当前唯一 UI 行为入口为 `tmp/20260907/Crystra-ui-design/components/shell-and-sidebar.md`，联读 MASTER、SearchField D19、组件优先 D18、布局规范、AC01/03/04 与 Sidebar v8 实际运行态。

用户随后明确：v8 HTML/CSS/JS/TS 是已实际核验的定稿实现，不只是参考。除页面整体 layout 的宿主挂载调整、Composer/Chat 接入和明确 mock/模拟行为的同义替换外，具体组件应完整复用 v8 与对应已有 components。不得继续按文档重新设计近似组件，也不得把 mock 数据复制为生产事实。该要求适用于全页面；Sidebar 是首个修正批次。

实测证据：`本地临时记录（未上传）`。1920×1080 下，v8 header 高 36px，当前 38px；v8 操作图标居中偏差 0，当前向左偏 6px；v8 标题 hover 为白色 8%，当前 5%；默认文字 v8 为 rgb(207,211,214)，当前为 rgb(249,250,251)。v8 focus 使用 2px 蓝色 outline，不能凭猜测改成另一套整行 focus 设计。

| 条款 | C094 实现差异 | 修正与验证要求 |
|---|---|---|
| 标题默认/hover/focus | 只验证背景非透明，未核对默认底色、整行 focus 和三个分区一致性 | 对照 v8 计算样式；分别测标题、空白、工具和键盘焦点 |
| 工具组隔离 | 无完整 pointer/mouse 事件隔离；CSS 多层覆盖 | 工具只亮自身，父行保持 idle；菜单不触发父 hover |
| 图标居中 | 全侧栏 `justify-content:flex-start` 覆盖 IconButton；尺寸规则重复 | 收紧文字按钮规则；逐按钮测 28×28 / 40×40 与图标中心偏差 |
| 抽屉 | 直接 display:none 和宽度跳变 | 340ms 固定内容宽度滑动裁切、途中可反向、reduced motion 即时 |
| 折叠品牌 | 不切换展开图标 | hover/focus 同槽替换；点击仅展开 |
| 提示 | 只有 aria-label | 真实 hover/focus tooltip，关闭时解除描述关联 |
| Escape | 搜索和目录同时关闭 | 内层优先；下一次关目录、焦点返回 rail |
| Section 偏好 | 临时目录强制展开并修改保存值 | 目录打开临时覆盖，关闭还原原偏好 |
| 搜索退出 | 放大镜只重新聚焦；外点可能抢回焦点 | 放大镜/关闭/Escape/外点一致清查询；外点不抢焦点 |
| 视图菜单 | 原生按钮拼接局部 CSS，缺少共享菜单焦点与键盘行为 | 复用已接受公共控件；保持排序/过滤偏好与版本身份 |
| 行与徽标 | 只接入 identity/title，缺状态/时间/关注投影 | 有权威数据才显示；缺数据不得伪造，不用 fixture 冒充宿主事实 |
| 设置 | C094 已保留 Crystra 打开现有宿主设置，但样式尚未完整对照 | 继续验证 footer 行/图标和弹层，不创建第二设置 owner |

执行：先记录 v8/当前页状态与计算样式，再补失败用例，再修改共享组件及 Shell 组合。完成后按相同场景并排验证，重新更新依赖候选；不继续用只断言“可见/背景非透明”的测试代替设计一致性。
